---
date: 2026-08-02
upstream_repo: https://github.com/obra/superpowers
upstream_version: post-v6.2.0
upstream_sha: 44c9b2d6e889982ac18c27d05a19fefe335194e1
previous_upstream_sha: 3dcbd5c4b48e02263fbf4a3c01e3fe4f81d584d9
ours_from_sha: 6f2d16f
ours_to_sha: pending
---

# Upstream Refresh — No Payload Change

Advances the tracked upstream from `3dcbd5c` to `44c9b2d`. A single upstream commit landed in the interval, and it does not touch the payload.

## Upstream coverage

`44c9b2d` — "docs: remove the 'We're Hiring' section from the README". Removes six lines from upstream's `README.md` announcing a community engineer opening that has since been filled.

No `skills/`, `commands/`, or other payload file changed upstream in this interval.

## Reconciliation decisions

- **Nothing to adopt.** This fork's `README.md` is pack-native and never carried upstream's hiring section (`rg -n 'Hiring' README.md` returns no matches), so there is nothing to remove.
- **SHA advanced anyway.** `changelogs/UPSTREAM_SHA` moves to `44c9b2d` so the next refresh diffs from the correct base instead of re-reporting this commit.

## Files affected

Modified:

- `changelogs/UPSTREAM_SHA`

## Verification notes

`git diff --stat 3dcbd5c..44c9b2d` reports `README.md | 6 ------`, and the removed block is absent from this fork's `README.md`. No payload validation is required beyond the standard `claude plugin validate` pair, which this refresh does not affect.
